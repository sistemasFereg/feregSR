class Vdate{

    static validacion(initDate, endDate, limitDate){
        if(initDate && endDate && limitDate){
            

        }
        else{
            return false;
        }
    }

}

export default Vdate;